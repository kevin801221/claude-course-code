# 專案環境變數範本 —— 複製成 .env 後填入真值
# .env 已進 .gitignore,絕不 commit(尤其 GEMINI_API_KEY)
#
# Gemini API key:只從環境變數讀,絕不寫進程式碼或 commit 進 repo。
# 申請:https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# 用的模型(app-spec 指定)
GEMINI_MODEL=gemini-3.5-flash

# 後端 FastAPI 服務埠(前端 AppConfig.backendBaseUrl 預設 127.0.0.1:8000)
PORT=8000

# --- 後端可選調參(通常不用動,留空即用程式預設) ---
# arXiv 收的分類(逗號分隔);預設 cs.AI,cs.LG,cs.CL,可加 cs.CV,cs.RO
# ARXIV_CATEGORIES=cs.AI,cs.LG,cs.CL
# 多抓幾篇再去重(預設 15)/ 去重後保留前幾篇(預設 5)
# ARXIV_FETCH_SIZE=15
# ARXIV_TOP_N=5
# 連續請求最小間隔秒數(arXiv ToU 要求 ≥3)
# ARXIV_REQUEST_INTERVAL_SEC=3
