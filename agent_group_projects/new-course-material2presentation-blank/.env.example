# 範例環境變數（複製成 .env 並填真值）
# DO NOT COMMIT .env
#
# 本模板預設不需要任何 API key（產出純 markdown）。
# 只有當你讓某個 skill 去呼外部服務時才需要，例如：
#
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
#
# Marp 轉投影片是純本機 CLI，不需要 key。
