# Collab Sync — IDE Extension ↔ CLI

> 這份檔案是 **Claude Code IDE Extension**（在編輯器裡寫 code）跟 **Claude Code CLI**（在終端跑 review / commit）之間的協作狀態檔。
>
> Extension 寫完一段 → 更新本檔 → CLI 端跑 `/sync` → review + commit + 更新本檔 → 再交回 Extension。
>
> 詳細協議見 `.claude/skills/cli-extension-sync/SKILL.md`。

## 當前狀態 (Current Status)
- **總體目標：** （依教學階段填）
- **當前階段：** 尚未開始

## Extension 的後續任務 (Next Tasks for IDE Extension)
- [ ] （等待 Extension 認領下一步）

## CLI 的後續任務 (Next Tasks for Claude Code CLI)
- [ ] 待 Extension 完成階段任務後，由 CLI 跑 `/sync` 進行 review + commit

## 待審查 (Pending Reviews)
- 無

## 反饋 (Feedback)
- 無

## 已完成任務 (Completed Tasks)
- 無
