# API 文件（由 docs-owner 隊友撰寫）

這個資料夾留給 agent team 的 docs-owner。

agent team 蓋完 Kanban 後，在這裡寫：
- `api.md` —— 每個 endpoint 的契約（method / path / request / response / 錯誤碼），
  以 backend-owner 在 team 裡最終定的契約為準。
- `usage.md` —— 怎麼啟動後端、怎麼開前端、怎麼操作看板。

規格方向見 `../SPEC.md`。
