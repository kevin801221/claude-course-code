# 引導 Claude Code 的上下文 (Prime Context)

使用 `tree` 指令來了解專案結構。

如果 `CLAUDE.md` 檔案存在，請先閱讀它以了解專案規範。

閱讀 `README.md` 檔案以了解專案的整體情況。

閱讀 `src/` 或根目錄中的關鍵檔案。

向我解釋：
- 專案結構
- 專案目的與目標
- 關鍵檔案及其用途
- 任何重要的依賴項 (Dependencies)
- 任何重要的配置檔案 (Configuration files)
